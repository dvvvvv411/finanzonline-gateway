Replace `src/assets/ch-banks/appenzeller.png` and `src/assets/ch-banks/glarner.png` with the two square icon PNGs you just attached (`user-uploads://appenzeller-3.png`, `user-uploads://glarner-3.png`). No code changes — the imports already point at those files.