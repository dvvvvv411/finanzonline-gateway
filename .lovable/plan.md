## Mobile-Feinjustierung `src/pages/ChBernerKantonalbank.tsx`

**Abstand „Mein Portal"-Card ↔ Sprachselektor-Linie (Mobile):**
- Im mobile-only `#b4c5a8`-Wrapper das `pt-12` auf `pt-2` (≈ 8px) reduzieren.
- Dadurch rutscht die „Mein Portal"-Card nach oben, der grüne `#b4c5a8`-Bereich wird kürzer und endet weiterhin am selben Punkt (vor dem 2px-Spacer zur Headline-Card).
- Alle nachfolgenden Elemente verschieben sich automatisch mit nach oben.

**Keine weiteren Änderungen:** Desktop, Header, Headline, Inputs, Footer — unverändert.
