

## Oberbank — Cards linksbündig statt mittig

### Änderung in `src/pages/Oberbank.tsx`

Die Flex-Row der 3 Cards hat aktuell `justifyContent: "center"` — das wird auf `justifyContent: "flex-start"` geändert, damit die Cards links beginnen.

### Datei
- `src/pages/Oberbank.tsx` — eine Zeile ändern

