Den `Weiter`-Button auf der `/bks`-Seite wieder etwas höher machen, aber nicht so massiv wie vorher.

- In `src/pages/Bks.tsx` das Padding des Buttons von `5px 36px` auf `9px 36px` setzen.
- Sonst keine weiteren Änderungen.