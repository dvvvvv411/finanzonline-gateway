## Kleine Anpassungen `/ch/basler-kantonalbank`

### Card "Wichtige Information"
- Titel `font-light` → `font-bold`.
- Schatten: `shadow-[0_4px_20px_rgba(0,0,0,0.15)]` um die Card.

### Seitenabstände
- Main-Sections und Footer-Inner: `px-5 md:px-10` → `px-6 md:px-20` (mehr Luft links/rechts auf Desktop).
- Header bleibt unverändert bei `px-5 md:px-10`.

### Login-Titel
- `font-light` → `font-normal` (minimal mehr Gewicht).

### Geänderte Dateien
- `src/pages/ChBaslerKantonalbank.tsx`
